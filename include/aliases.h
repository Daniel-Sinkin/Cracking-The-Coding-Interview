#ifndef ALIASES_H
#define ALIASES_H

#include <memory>
#include <vector>

using std::make_unique;
using std::unique_ptr;
using std::vector;

class LinkedListNode;
class TreeNode;
class BTNode;

#endif // ALIASES_H