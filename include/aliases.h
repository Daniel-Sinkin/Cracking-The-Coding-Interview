#ifndef ALIASES_H
#define ALIASES_H

#include <algorithm>
#include <memory>
#include <string>
#include <vector>

using std::make_unique;
using std::string;
using std::unique_ptr;
using std::vector;

#endif // ALIASES_H